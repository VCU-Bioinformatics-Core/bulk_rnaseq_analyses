/*
========================================================================================
    Nextflow config file for nf-test runs (Phase 6.2)
========================================================================================
*/

// Inherit defaults from the parent module config
includeConfig '../nextflow.config'

// Suppress per-run HTML report + trace files inside nf-test's ephemeral
// work dirs; they add noise without value during testing.
report.enabled = false
trace.enabled  = false
