% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plots_qc.R
\name{qc_hclust_density}
\alias{qc_hclust_density}
\title{Hierarchical clustering dendrogram + log-CPM density per sample}
\usage{
qc_hclust_density(
  tmm,
  sample_info,
  fig_path,
  dist_method = "euclidean",
  clust_method = "ward.D2"
)
}
\arguments{
\item{tmm}{TMM-normalized counts (Genes x Samples), e.g. \code{\link[=run_tmm]{run_tmm()}}
output.}

\item{sample_info}{Same shape as elsewhere (\code{sample} + \code{condition}).}

\item{fig_path}{Output PNG path.}

\item{dist_method}{Distance metric for \code{dist()}. Default \code{"euclidean"}.}

\item{clust_method}{Linkage for \code{hclust()}. Default \code{"ward.D2"}.}
}
\value{
Invisibly returns \code{fig_path}.
}
\description{
Two side-by-side panels via \code{patchwork}:
\itemize{
\item Left: Ward dendrogram on \code{dist(t(log2(tmm + 1)))}. Sample labels
are colored by group so visually-clustered replicates can be
sanity-checked at a glance.
\item Right: per-sample density curve of \code{log2(CPM + 1)}, colored by
group. Distributions should overlap; an outlier curve flags a
library prep / depth / contamination problem.
}
}
