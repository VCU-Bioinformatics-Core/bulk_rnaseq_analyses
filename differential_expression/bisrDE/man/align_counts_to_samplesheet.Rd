% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/io.R
\name{align_counts_to_samplesheet}
\alias{align_counts_to_samplesheet}
\title{Align a count matrix to a samplesheet}
\usage{
align_counts_to_samplesheet(counts, samplesheet, sample_col = "SampleID")
}
\arguments{
\item{counts}{A count \code{data.frame} or matrix (Genes x Samples), typically
from \code{\link[=read_counts]{read_counts()}}.}

\item{samplesheet}{Data frame from \code{\link[=read_samplesheet]{read_samplesheet()}}.}

\item{sample_col}{Column in \code{samplesheet} holding sample identifiers
(default \code{"SampleID"}).}
}
\value{
The input \code{counts} reordered and subset to follow \code{samplesheet}
row order, with \code{make.names()}-munged sample IDs as column names.
}
\description{
Subset and reorder the columns of a count matrix to match the
samples declared in the samplesheet. The samplesheet is the source of
truth — if it lists samples missing from the counts, this errors; if
the counts carry extra samples, those are dropped with a \code{cli} info
message.
}
\details{
\code{read_counts()} indirectly applies \code{make.names()} to sample IDs
(via \code{data.frame(..., row.names = 1)} on tibble-style input), so this
function applies the same transform to the samplesheet IDs before
intersecting. Without this alignment, \code{DESeq2::DESeqDataSetFromMatrix}
fails with \verb{ncol(countData) == nrow(colData) is not TRUE} when the
upstream merged-counts file carries samples beyond the scope of the
current DE run.
}
