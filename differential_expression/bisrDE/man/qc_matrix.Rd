% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/normalize.R
\name{qc_matrix}
\alias{qc_matrix}
\title{Log-scale expression matrix for sample-level QC (PCA, correlation)}
\usage{
qc_matrix(dds, tmm)
}
\arguments{
\item{dds}{A \code{DESeqDataSet} (post pre-filter, pre-\code{DESeq()}).}

\item{tmm}{TMM-CPM matrix from \code{\link[=run_tmm]{run_tmm()}} (the fallback input).}
}
\value{
A numeric Genes x Samples matrix with attribute \code{"transform"} set
to \code{"vst"} or \code{"log2_tmm_cpm"}.
}
\description{
The matrix every sample-exploration view should be computed
on: the blind variance-stabilising transform of the pre-filtered counts
(\code{run_vst(dds, blind = TRUE)}, all genes), or, when VST cannot be
computed (tiny fixtures), \code{log2(TMM-CPM + 1)}. Never the linear TMM-CPM
matrix, whose PCA is dominated by a handful of highly expressed genes.
}
