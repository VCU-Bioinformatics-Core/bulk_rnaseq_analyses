% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/report.R
\name{generate_report}
\alias{generate_report}
\title{Render the bulk RNA-seq DE report}
\usage{
generate_report(
  rds_path,
  output_dir,
  brs_ticket = "",
  analyst = "Mikail Bala",
  report_prefix = "rnaseq_analysis"
)
}
\arguments{
\item{rds_path}{Path to the analysis RDS produced by \code{\link[=run_pipeline]{run_pipeline()}}.
Stores \code{list(results, comparisons, out_dirs, pca_plot, pca_plotly, pca_3d, annotation)}.}

\item{output_dir}{Directory where the rendered HTML will be written.
Created if it does not exist.}

\item{brs_ticket}{Optional BRS ticket identifier. Default \code{""}. When
non-empty, rendered as a \verb{subtitle:} line under the report title.}

\item{analyst}{Analyst name displayed in the Overview section.
Default \code{"Mikail Bala"}.}

\item{report_prefix}{Prefix for the output HTML filename. Default
\code{"rnaseq_analysis"}. Combined with a timestamp at render time.}
}
\value{
Absolute path to the rendered HTML file.
}
\description{
Render the package's Quarto template (\code{inst/qmd/report.qmd})
to a self-contained HTML file. Inputs come from the RDS bundle saved
by \code{\link[=run_pipeline]{run_pipeline()}}.
}
\details{
Renders by:
\enumerate{
\item Locating the qmd template via \code{system.file("qmd/report.qmd", package = "bisrDE")}.
\item Copying the template + the \verb{_sections/} child folder into a
tempdir.
\item Substituting \verb{\\\{\\\{SUBTITLE_LINE\\\}\\\}} in the YAML preamble with
either \code{subtitle: "<brs_ticket>"} (if non-empty) or an empty
string (no stray YAML field).
\item Calling \code{quarto::quarto_render} with \code{execute_params} =
\code{list(rds_path, analyst, brs_ticket, genome_assembly, sections_dir)}.
\item Moving the rendered HTML to \code{output_dir} with a timestamped
filename.
}
\code{genome_assembly} is derived from the RDS's \code{annotation} slot
(\code{"human"} -> \code{"GRCh38 human primary assembly"},
\code{"mouse"} -> \code{"GRCm39 mouse primary assembly"}).
}
