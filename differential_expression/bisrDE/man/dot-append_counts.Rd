% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils.R
\name{.append_counts}
\alias{.append_counts}
\title{Append per-sample normalized counts to a DE results table}
\usage{
.append_counts(df, ids, mat, orig_ids, prefix)
}
\arguments{
\item{df}{Annotated DE data frame.}

\item{ids}{Character vector (length \code{nrow(df)}) of each row's gene ID in the
matrix's row-name namespace (see \code{\link[=.match_id_column]{.match_id_column()}}).}

\item{mat}{Normalized-count matrix (genes x samples).}

\item{orig_ids}{Named vector mapping munged SampleID -> original SampleID.}

\item{prefix}{Column-name prefix, e.g. \code{"TMM"} or \code{"DESeq2norm"}.}
}
\value{
\code{df} with the per-sample count columns appended (\code{NA} for any gene
absent from \code{mat}).
}
\description{
Join a normalized-count matrix onto an annotated DE data frame
by gene, adding one \verb{<prefix>_<SampleID>} column per sample so the DE
spreadsheet carries expression values next to the log2FC. Matrix columns
(which are \code{make.names()}-munged SampleIDs) are renamed back to the
original SampleIDs via \code{orig_ids}.
}
\keyword{internal}
