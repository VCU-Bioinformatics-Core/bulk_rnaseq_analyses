% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/normalize.R
\name{run_tmm}
\alias{run_tmm}
\title{TMM-normalize a count matrix}
\usage{
run_tmm(counts)
}
\arguments{
\item{counts}{A count matrix or data frame (Genes x Samples) of
integer-coercible counts. Typically the output of
\code{\link[=align_counts_to_samplesheet]{align_counts_to_samplesheet()}} cast to integer.}
}
\value{
A numeric matrix of TMM-normalized counts-per-million (Genes x
Samples), preserving rownames and colnames of the input.
}
\description{
Wrap edgeR's TMM normalization (DGEList -> calcNormFactors ->
cpm) into a single call. Returns the cpm matrix; the intermediate
\code{DGEList} is discarded since downstream code only consumes the cpm
output.
}
\details{
Persisting the result to disk is the caller's responsibility
(the parent \code{de.R} writes a \verb{normalizedCounts_tmm<DATE>.csv}; the
package leaves that as a pipeline-orchestrator concern). If
normalization-factor inspection becomes a need later, extend the
signature with a \code{return_dge = FALSE} flag rather than changing the
default return shape.
}
