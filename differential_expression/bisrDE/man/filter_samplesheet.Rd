% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/io.R
\name{filter_samplesheet}
\alias{filter_samplesheet}
\title{Drop excluded samples / groups from a samplesheet}
\usage{
filter_samplesheet(
  samplesheet,
  exclude_samples = NULL,
  exclude_groups = NULL,
  sample_col = "SampleID",
  group_col = "GroupID"
)
}
\arguments{
\item{samplesheet}{Data frame from \code{\link[=read_samplesheet]{read_samplesheet()}}.}

\item{exclude_samples}{Optional character vector of \code{SampleID}s to drop.}

\item{exclude_groups}{Optional character vector of \code{GroupID}s to drop.}

\item{sample_col}{Sample-ID column name (default \code{"SampleID"}).}

\item{group_col}{Group-ID column name (default \code{"GroupID"}).}
}
\value{
The samplesheet with excluded rows removed (row order otherwise
preserved). Errors via \code{cli::cli_abort} if fewer than two samples
remain.
}
\description{
Remove rows from a samplesheet so they are dropped from the
entire analysis (contrasts, normalization, DESeq2, plots). Three
exclusion sources are combined (union):
\enumerate{
\item An optional declarative \code{Exclude} column — any row whose value
is \code{1} / \code{TRUE} / \code{"yes"} (case-insensitive) is dropped. Good
for a permanent "this sample was contaminated" record kept with
the samplesheet.
\item \code{exclude_samples} — \code{SampleID}s to drop (ad-hoc, e.g. a CLI
flag) without editing the canonical samplesheet.
\item \code{exclude_groups} — every sample whose \code{GroupID} matches is
dropped.
}
}
\details{
The \code{SampleID} join key is untouched — exclusion only removes
rows, so downstream \code{make.names()}-based alignment to the count matrix
is unaffected for the surviving samples.
}
