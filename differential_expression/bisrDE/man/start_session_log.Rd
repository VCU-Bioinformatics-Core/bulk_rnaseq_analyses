% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cli.R
\name{start_session_log}
\alias{start_session_log}
\title{Start a tee'd session log for the current pipeline run}
\usage{
start_session_log(log_dir)
}
\arguments{
\item{log_dir}{Directory where the session log will be written. Created
if it does not exist.}
}
\value{
A list with \code{path} (file path of the log) and \code{con} (open file
connection). The caller MUST close the connection at end-of-run via
\code{\link[=stop_session_log]{stop_session_log()}}.
}
\description{
Open a \code{sink(split = TRUE)} on the output stream so every
\verb{cli::*}, \code{print()}, and \code{cat()} call from this point on is mirrored
to BOTH the terminal AND a timestamped session log file.
}
\details{
R's \code{message()} / \code{warning()} / \code{stop()} channel is NOT split
(R's \code{sink} does not support \code{split = TRUE} for message-type sinks);
those still print to terminal but are not captured by this log. For
the analytical pipeline this is fine: \verb{cli::*} writes to stdout.
}
