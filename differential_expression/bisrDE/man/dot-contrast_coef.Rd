% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/de.R
\name{.contrast_coef}
\alias{.contrast_coef}
\title{Name of the DESeq2 model coefficient for \code{exp} vs \code{ctrl}}
\usage{
.contrast_coef(dds, exp, ctrl)
}
\arguments{
\item{dds}{A \code{DESeqDataSet} after \code{DESeq()}.}

\item{exp, ctrl}{Group labels.}
}
\value{
The coefficient name, or \code{NULL} if none matches.
}
\description{
After \code{relevel(ref = ctrl)} the coefficient DESeq2 names
\verb{condition_<exp>_vs_<ctrl>} estimates the contrast directly, which is
what \code{lfcShrink(type = "apeglm")} needs (it only accepts \verb{coef =}).
DESeq2 may \code{make.names()} the level labels, so both spellings are tried.
}
\keyword{internal}
