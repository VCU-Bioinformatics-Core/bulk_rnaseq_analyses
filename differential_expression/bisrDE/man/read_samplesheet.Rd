% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/io.R
\name{read_samplesheet}
\alias{read_samplesheet}
\title{Read a comma-delimited samplesheet}
\usage{
read_samplesheet(path)
}
\arguments{
\item{path}{Path to the samplesheet CSV.}
}
\value{
A \code{data.frame} with \code{stringsAsFactors = FALSE}, suitable as input
to \code{\link[=parse_contrasts]{parse_contrasts()}} and \code{\link[=align_counts_to_samplesheet]{align_counts_to_samplesheet()}}.
}
\description{
Read the BISR per-run samplesheet into a data frame. The
samplesheet must have at least three columns: \code{SampleID}, \code{GroupID},
then one column per contrast (1 = experimental, 0 = control, NA =
excluded from that contrast).
}
\details{
Locked to comma-delimited input by design (matches how
\code{run_analysis.sh} invokes the pipeline). If TSV samplesheets become a
real need, extend the API rather than auto-detecting.
}
