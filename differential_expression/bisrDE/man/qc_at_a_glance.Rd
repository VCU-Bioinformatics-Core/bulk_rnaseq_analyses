% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/qc_summary.R
\name{qc_at_a_glance}
\alias{qc_at_a_glance}
\title{Compute a short, data-driven QC verdict for the run}
\usage{
qc_at_a_glance(counts, qc_mat, sample_info, transform = NULL)
}
\arguments{
\item{counts}{Raw Genes x Samples count matrix (post alignment; the same
one the library-size barplot uses).}

\item{qc_mat}{Log-scale expression matrix from \code{\link[=qc_matrix]{qc_matrix()}}.}

\item{sample_info}{Data frame with \code{sample} and \code{condition} columns.}

\item{transform}{Label of the transform behind \code{qc_mat} (\code{"vst"} or
\code{"log2_tmm_cpm"}); read from \code{attr(qc_mat, "transform")} when \code{NULL}.}
}
\value{
A list: \code{libsize_M} (named numeric) and \code{libsize_M_range},
\code{depth_fold} (max/min), \code{detected} (named integer) and
\code{detected_range}, \code{pc_var} (numeric, PC1 and PC2 percent variance),
\code{n_groups}, \code{separation} (ratio of mean between-group centroid distance
to mean within-group distance on PC1-PC2; \code{NA} with one group or no
estimable within-group spread), \code{silhouette} (mean silhouette width on
PC1-PC2 using group labels; \code{NA} when not computable), \code{outliers}
(character, see Details), \code{outlier_ratio} (named numeric, each flagged
sample's nearest-replicate distance over the typical one), \code{transform},
\code{n_genes}, and \code{text} (one paragraph).
}
\description{
Turns the sample-exploration figures into numbers a reader
can act on: sequencing-depth range, detected-gene range, variance
captured by PC1/PC2, whether the groups separate on PC1-PC2, and which
samples sit unusually far from their own group. The report prints the
returned \code{text}; the pieces are also stored in the run JSON.
}
\details{
Separation is descriptive, not a test: a ratio well above 1
means the groups are farther apart than replicates are from each other.
Outliers are scored by the distance to the NEAREST replicate of the same
group on PC1-PC2 (not to the group centroid, which an outlier drags
toward itself so that in a 3-vs-3 design nothing could ever be flagged).
A sample is flagged when that distance is more than 3 robust SDs
(median + 3 MAD over all samples) beyond the typical nearest-replicate
distance AND at least twice the typical distance. In a group of two the
pair simply disagrees and both samples are flagged. Flags are something
to inspect, not a reason to drop a sample.
}
