% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/enrich.R
\name{process_msigdb_hallmark}
\alias{process_msigdb_hallmark}
\title{MSigDB Hallmark gene set GSEA}
\usage{
process_msigdb_hallmark(
  annotated_result,
  p = 1,
  annotation = get0("annotation", envir = .GlobalEnv),
  annotation_db = get0("annotation_db", envir = .GlobalEnv)
)
}
\arguments{
\item{annotated_result}{Annotated DESeq2 results frame (output of
\code{\link[=annotate_results]{annotate_results()}}). Must contain \code{ENTREZID}, \code{log2FoldChange},
\code{baseMean}.}

\item{p}{Adjusted p-value cutoff. Default \code{1}.}

\item{annotation}{One of "human" or "mouse". Defaults to the
\code{annotation} global.}

\item{annotation_db}{\code{OrgDb} for \code{setReadable} Symbol mapping.
Defaults to the \code{annotation_db} global.}
}
\value{
setReadable'd GSEA object, or \code{NULL} if no enriched gene sets
or any error.
}
\description{
GSEA over the MSigDB Hallmark collection (50 well-curated
gene sets). MSigDB has no dedicated GSEA wrapper the way GO/KEGG/
Reactome do; this builds a \code{TERM2GENE} table from
\code{msigdbr::msigdbr(species, collection = "H")} and passes it to
\code{clusterProfiler::GSEA()}.
}
\details{
Targets msigdbr ≥10.0 (renv lock has v26.1.0) — uses
\code{species}/\code{collection} args and the \code{ncbi_gene} Entrez column. The
pre-v10 args (\code{category} + \code{entrez_gene} column) are deprecated but
still functional; the modern names avoid deprecation warnings on
every run.
}
