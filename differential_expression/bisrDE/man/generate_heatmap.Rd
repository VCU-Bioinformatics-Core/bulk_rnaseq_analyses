% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plots_heatmap.R
\name{generate_heatmap}
\alias{generate_heatmap}
\title{Heatmap of differentially expressed genes}
\usage{
generate_heatmap(
  results_df,
  normalized_counts,
  sample_info,
  p = 0.05,
  lfc = 0.58,
  exp_name,
  ctrl_name,
  top_n = NULL
)
}
\arguments{
\item{results_df}{Data frame containing (annotated) DESeq2 results. If a
\code{SYMBOL} column is present and \code{top_n} is set, gene symbols are used
as row labels.}

\item{normalized_counts}{Genes x Samples numeric matrix of normalized
counts (typically the output of \code{\link[=run_tmm]{run_tmm()}}).}

\item{sample_info}{Data frame with at least two columns: \code{sample}
(canonical IDs matching the \code{make.names()} form of
\code{colnames(normalized_counts)}) and \code{condition} (group label).}

\item{p}{P-value threshold for significance. Default \code{0.05}.}

\item{lfc}{Log2 fold change threshold. Default \code{0.58} (≈1.5x).}

\item{exp_name}{Experimental group name (or vector of names).}

\item{ctrl_name}{Control group name (or vector of names).}

\item{top_n}{If non-NULL, take the top N genes by ascending \code{padj} after
significance filtering, and label rows by gene \code{SYMBOL} when
available. If \code{NULL} (default), include every gene that passes
thresholds with no row labels.}
}
\value{
Invisibly \code{NULL} if filtering yields fewer than 2 samples or 2
genes; otherwise the side-effect is the rendered heatmap (printed to
the active graphics device).
}
\description{
Render a z-score heatmap of DE-significant genes restricted
to the samples that belong to the current comparison's experimental
and control groups. Has two modes via \code{top_n}: \code{NULL} (the default)
keeps every gene that passes thresholds with no row labels;
\code{top_n = 100} (or any integer) takes the top-N genes by ascending
\code{padj} and labels rows by gene \code{SYMBOL}.
}
\details{
Restricts the count matrix to the comparison's samples (fixes
the prior bug where heatmap columns showed every sample in the full
samplesheet). Z-scores each gene on the log scale,
\code{t(scale(t(log2(values + 1))))}: per-row scaling already makes every
gene scale-free, but on the linear scale one sample at 10x expression
compresses the gene's other samples to the same colour; the log step
keeps within-row differences visible. A gene with zero variance across
the shown samples gets z = 0 rather than \code{NaN}. Renders with a
blue-white-red palette via \code{gplots::heatmap.2}.
}
