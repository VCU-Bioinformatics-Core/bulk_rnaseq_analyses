% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils.R
\name{.display_lookup}
\alias{.display_lookup}
\title{Build a munged-SampleID -> display-label lookup}
\usage{
.display_lookup(sample_info)
}
\arguments{
\item{sample_info}{Data frame with at least a \code{sample} column; optionally
a \code{display} column.}
}
\value{
Named character vector: names are \code{make.names(sample_info$sample)},
values are the display labels (falling back to the munged ID per cell
when a \code{display} entry is blank/\code{NA}).
}
\description{
Construct a named character vector mapping each sample's
\code{make.names()}-munged ID (the form used as count-matrix column names
and plot label keys) to the label that should be shown on plots.

When \code{sample_info} carries a \code{display} column (populated from the
samplesheet's optional \code{DisplayName} column), those values are used.
Otherwise the lookup is the identity on the munged ID, so plots render
exactly as they did before display names existed.
}
\keyword{internal}
