% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/normalize.R
\name{run_vst}
\alias{run_vst}
\title{Variance-stabilized transform with a robust fallback}
\usage{
run_vst(dds, blind = TRUE)
}
\arguments{
\item{dds}{A \code{DESeqDataSet}. Typically post-pre-filter, pre-\code{DESeq()} —
\code{blind = TRUE} is the default to keep the transform design-agnostic
(appropriate for sample-level QC across the whole experiment, not for
any one contrast).}

\item{blind}{Passed through to \code{DESeq2::vst()} /
\code{DESeq2::varianceStabilizingTransformation()}. Default \code{TRUE}.}
}
\value{
A \code{DESeqTransform} object on success, or \code{NULL} if both the fast
path and the fallback fail. Callers should branch on \code{is.null()} and
skip downstream vst-dependent steps rather than abort the run.
}
\description{
Wrap \code{DESeq2::vst()} with a \code{tryCatch} that falls back to
\code{DESeq2::varianceStabilizingTransformation()} when the fast vst path
fails. The fast path subsamples 1000 genes for the dispersion fit and
raises an error on small/sparse datasets; the fallback is slower but
always-correct.
}
