% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plots_qc.R
\name{qc_vst_dist_heatmap}
\alias{qc_vst_dist_heatmap}
\title{Sample x Sample vst distance heatmap}
\usage{
qc_vst_dist_heatmap(dds, sample_info, fig_path)
}
\arguments{
\item{dds}{A \code{DESeqDataSet} (typically post pre-filtering, before
per-comparison \code{DESeq()} calls). The vst is \code{blind = TRUE} so the
transform is design-agnostic.}

\item{sample_info}{Same shape as in \code{\link[=qc_correlation_heatmap]{qc_correlation_heatmap()}}.}

\item{fig_path}{Output PNG path.}
}
\value{
Invisibly returns \code{fig_path} on success, or \code{NULL} if vst
can't be computed (e.g. <2 samples, all-zero genes, \code{run_vst}
fallback fails).
}
\description{
Standard DESeq2-vignette QC plot: variance-stabilizing
transform on raw counts, then \code{dist()} on the transposed matrix to
get sample x sample Euclidean distances, then visualize via
\code{ComplexHeatmap}. Replicates that don't cluster together flag
potential outliers or mis-labelling.
}
\details{
Delegates the vst computation to \code{\link[=run_vst]{run_vst()}}, which wraps
\code{DESeq2::vst()} with a \code{tryCatch} fallback to
\code{DESeq2::varianceStabilizingTransformation()}. The fast vst path
subsamples 1000 genes and fails on small/sparse datasets; the
fallback is slower but always-correct.
}
