% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/enrich.R
\name{process_reactome_gsea}
\alias{process_reactome_gsea}
\title{Reactome pathway Gene Set Enrichment Analysis}
\usage{
process_reactome_gsea(
  annotated_result,
  p = 1,
  annotation = get0("annotation", envir = .GlobalEnv),
  annotation_db = get0("annotation_db", envir = .GlobalEnv)
)
}
\arguments{
\item{annotated_result}{Annotated DESeq2 results frame (output of
\code{\link[=annotate_results]{annotate_results()}}). Must contain \code{ENTREZID}, \code{log2FoldChange},
and \code{baseMean} columns.}

\item{p}{Adjusted p-value cutoff. Default \code{1}.}

\item{annotation}{One of "human" or "mouse". Defaults to the
\code{annotation} global set during option parsing.}

\item{annotation_db}{\code{OrgDb} for \code{setReadable} Symbol mapping.
Defaults to the \code{annotation_db} global.}
}
\value{
setReadable'd \code{gsePathway} object, or \code{NULL} if no enriched
pathways or any error.
}
\description{
GSEA over Reactome pathways. Wraps \code{ReactomePA::gsePathway}
directly with the \code{--annotation} value (Reactome accepts "human"
and "mouse" natively, so no organism-code translation is needed —
unlike KEGG).
}
