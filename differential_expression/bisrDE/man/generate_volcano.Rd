% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plots_volcano.R
\name{generate_volcano}
\alias{generate_volcano}
\title{Volcano plot of differential expression results}
\usage{
generate_volcano(
  data,
  exp_name,
  ctrl_name,
  p = 0.05,
  lfc = 0.58,
  sig = "padj",
  n_labels = 10,
  use_shrunken = TRUE
)
}
\arguments{
\item{data}{Annotated DESeq2 results data frame (output of
\code{\link[=annotate_results]{annotate_results()}}). Must contain \code{log2FoldChange}, \code{ENSEMBL_ID},
\code{SYMBOL}, and the \code{sig} column.}

\item{exp_name}{Experimental group name (rendered in the title).}

\item{ctrl_name}{Control group name (rendered in the title).}

\item{p}{P-value threshold for significance. Default \code{0.05}.}

\item{lfc}{Log2 fold change threshold. Default \code{0.58} (≈1.5x).}

\item{sig}{Column name for significance values. Default \code{"padj"}. The
same column is used for the colored category, the y-axis transform,
the threshold line, and the top-genes-label selection.}

\item{n_labels}{Maximum number of genes to label with \code{ggrepel}: the top
\code{n_labels} most-significant genes (by ascending \code{sig}) that pass both
thresholds. Default \code{10} — keeps the plot readable when many genes are
significant. Configurable via the \code{--volcano-labels} CLI flag.}

\item{use_shrunken}{When \code{TRUE} (default) and \code{data} carries a
\code{log2FC_shrunken} column (see \code{\link[=perform_deseq2_analysis]{perform_deseq2_analysis()}} with
\code{lfc_shrink}), points are placed at the shrunken log2 fold change. The
colour (the DEG call) and the dashed threshold lines still use the MLE
\code{log2FoldChange}, so a significant gene can sit inside the lines: that
is the shrinkage showing a noisy estimate. \code{FALSE} or no such column:
the x axis is the MLE.}
}
\value{
A \code{ggplot} object.
}
\description{
Render a volcano plot from an annotated DESeq2 results
data frame. Color, y-axis, threshold line, and label-selection all use
the same significance field (\code{sig}, default \code{"padj"}) so the dotted
threshold line aligns with the grey/colored boundary.
}
\details{
The top \code{n_labels} most-significant genes (by ascending \code{sig}) are
labeled with their \code{SYMBOL}. Color tags (colorblind-safe): "Over expressed"
(vermillion), "Under expressed" (blue), "Not significant" (grey70).
}
