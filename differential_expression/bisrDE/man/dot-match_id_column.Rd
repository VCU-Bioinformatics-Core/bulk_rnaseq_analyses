% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils.R
\name{.match_id_column}
\alias{.match_id_column}
\title{Pick the DE-table ID column that matches a count matrix's row names}
\usage{
.match_id_column(df, target)
}
\arguments{
\item{df}{Annotated DE data frame.}

\item{target}{Character vector to match against (e.g. \code{rownames(counts)}).}
}
\value{
Name of the best-matching column, or \code{NULL} if none overlaps.
}
\description{
A count matrix is keyed by the INPUT gene IDs (ensembl /
symbol / entrez, per \code{--id-type}), which the annotated DE table stores
under \code{"<KEYTYPE>_ID"}. Downstream subsetting that hardcodes \code{ENSEMBL_ID}
silently drops every gene for symbol/entrez inputs. This returns the
candidate ID column of \code{df} with the most overlap with \code{target}, so the
right key is used regardless of id_type.
}
\keyword{internal}
