% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/enrich.R
\name{.gsea_rank_vector}
\alias{.gsea_rank_vector}
\title{Build the ranked gene vector for pre-ranked GSEA}
\usage{
.gsea_rank_vector(annotated_result, id_col, metric = c("stat", "log2fc"))
}
\arguments{
\item{annotated_result}{Annotated DESeq2 results frame.}

\item{id_col}{Column holding the gene identifier to name the vector by
(e.g. \code{"ENSEMBL_ID"} or \code{"ENTREZID"}).}

\item{metric}{One of \code{"stat"}, \code{"log2fc"}. If \code{"stat"} is requested but
the column is absent, falls back to \code{"log2fc"} with a warning.}
}
\value{
A named numeric vector sorted decreasing, one entry per unique ID
(first occurrence kept). Ties are broken by ID so the order is
deterministic. Attribute \code{"metric"} records the metric actually used.
}
\description{
One place that decides how genes are ranked for every GSEA
backend. \code{metric = "stat"} (default) ranks by the DESeq2 Wald statistic,
which combines effect size and evidence and keeps noisy low-count genes
with large fold changes out of the tails; \code{metric = "log2fc"} reproduces
the pre-v1.7 behaviour of ranking by the unshrunken log2 fold change.
}
\keyword{internal}
