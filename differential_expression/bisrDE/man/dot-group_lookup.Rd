% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils.R
\name{.group_lookup}
\alias{.group_lookup}
\title{Build a munged-SampleID -> condition (group) lookup}
\usage{
.group_lookup(sample_info)
}
\arguments{
\item{sample_info}{Data frame with \code{sample} and \code{condition} columns.}
}
\value{
Named character vector: names are \code{make.names(sample_info$sample)},
values are the group labels.
}
\description{
Named vector mapping each sample's \code{make.names()}-munged ID
to its \code{condition}/group label. Use this to align group labels to a
matrix's column order (or a \code{prcomp} rowname order) rather than
relying on \code{sample_info} row order matching the data column order.
}
\keyword{internal}
