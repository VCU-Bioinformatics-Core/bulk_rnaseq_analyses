% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pipeline.R
\name{run_pipeline}
\alias{run_pipeline}
\title{Top-level bulk RNA-seq DE pipeline}
\usage{
run_pipeline(
  counts_path,
  samplesheet_path,
  outdir,
  runid,
  annotation,
  id_type = "ensembl",
  brs_ticket = "",
  exclude_samples = NULL,
  exclude_groups = NULL,
  include_contrasts = NULL,
  exclude_contrasts = NULL,
  volcano_labels = 10,
  padj = 0.05,
  fold_change = 1.5,
  session_log = NULL,
  independent_filtering = FALSE,
  gsea_rank = c("stat", "log2fc"),
  lfc_shrink = c("apeglm", "normal", "none")
)
}
\arguments{
\item{counts_path}{Path to the merged counts TSV (typically
\code{rsem.merged.gene_counts.tsv} from nf-core/rnaseq).}

\item{samplesheet_path}{Path to the comma-delimited samplesheet.}

\item{outdir}{Base output directory. Subdirectories created via
\code{\link[=setup_directories]{setup_directories()}}.}

\item{runid}{Run identifier (used in cli output and downstream report
metadata).}

\item{annotation}{One of \code{"human"} or \code{"mouse"}. Drives both organism
dispatch in the enrich fns and the OrgDb selection.}

\item{id_type}{One of \code{"ensembl"}, \code{"entrez"}, \code{"symbol"}. Identifies
the gene-ID type used as count rownames. Default \code{"ensembl"}.}

\item{brs_ticket}{Optional BRS ticket identifier (e.g. \code{"BRS-1234"}).
Default \code{""}. Currently passed through into the returned artifact
list for the report renderer.}

\item{exclude_samples}{Optional character vector of \code{SampleID}s to drop
from the entire analysis (see \code{\link[=filter_samplesheet]{filter_samplesheet()}}). Default \code{NULL}.}

\item{exclude_groups}{Optional character vector of \code{GroupID}s to drop.
Default \code{NULL}.}

\item{include_contrasts}{Optional character vector. When non-NULL, only
these contrast columns are processed (allowlist). Default \code{NULL}.}

\item{exclude_contrasts}{Optional character vector of contrast columns to
skip (denylist). Default \code{NULL}.}

\item{volcano_labels}{Max genes to label on each volcano plot (the top N by
significance). Default \code{10}. Passed through to \code{\link[=generate_volcano]{generate_volcano()}}.}

\item{padj}{Adjusted p-value (FDR) cutoff for calling DEGs. Default \code{0.05}.}

\item{fold_change}{Linear fold-change cutoff for calling DEGs (e.g. \code{2} for
2-fold). Default \code{1.5}; converted to a log2 cutoff internally and applied
to the volcano / heatmaps / DEG counts / report text.}

\item{session_log}{Optional session-log handle from \code{\link[=start_session_log]{start_session_log()}}.
When supplied, this call uses it (so a driver can make one log span both
the analysis and the report render) and does NOT close it — the caller
owns the lifecycle. When \code{NULL} (default) the pipeline opens and closes
its own log, as before. Default \code{NULL}.}

\item{independent_filtering}{DESeq2 \code{results(independentFiltering =)}.
Default \code{FALSE} (the historical BISR setting; DESeq2's default is
\code{TRUE}). Recorded in the run JSON and stated in the report Methods.}

\item{gsea_rank}{Ranking metric for pre-ranked GSEA: \code{"stat"} (default,
the DESeq2 Wald statistic) or \code{"log2fc"} (pre-v1.7 behaviour).}

\item{lfc_shrink}{Log2 fold-change shrinkage: \code{"apeglm"} (default; falls
back to \code{"normal"} with a message when apeglm is not installed),
\code{"normal"}, or \code{"none"}. Adds \code{log2FC_shrunken} / \code{lfcSE_shrunken} to
the DE tables and places volcano points at the shrunken value; DEG
calls always use the MLE \code{log2FoldChange}.}

\item{tick}{Optional function of one argument (a step label) called before
each of the \link{.STEPS_PER_COMPARISON} phases, so the caller can advance a
progress bar mid-comparison. \code{NULL} (default) disables it.}
}
\value{
Invisibly, a named list of pipeline artifacts:
\code{results}, \code{comparisons}, \code{out_dirs}, \code{pca_plot} (ggplot),
\code{pca_plotly} (plotly 2D), \code{pca_plotly_3d} (plotly 3D),
\code{annotation}, \code{runid}, \code{brs_ticket}, \code{sample_info}, \code{tmm},
\code{counts}, \code{dds}, \code{rds_path} (path to the saved analysis RDS).
}
\description{
End-to-end bulk RNA-seq differential expression pipeline:
read counts + samplesheet, parse contrasts, TMM normalize, run DESeq2
per comparison (with GO/KEGG/Reactome/MSigDB enrichment), produce
sample-level QC plots and PCA, save an RDS bundle for downstream
reporting.
}
\details{
Loads the appropriate OrgDb (\code{org.Hs.eg.db} or \code{org.Mm.eg.db})
via \code{requireNamespace} — both packages are listed in \verb{Suggests:},
not \verb{Imports:}, since each run only needs one. Stops with a clear
message if the required OrgDb is not installed.

Stops at the RDS save. Report rendering is a separate concern (Phase
5.6 will introduce \code{bisrDE::generate_report()}); for now, downstream
callers handle report generation themselves.
}
