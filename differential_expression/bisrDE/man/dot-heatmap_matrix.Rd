% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plots_heatmap.R
\name{.heatmap_matrix}
\alias{.heatmap_matrix}
\title{Build the z-score matrix and row labels for \code{generate_heatmap}}
\usage{
.heatmap_matrix(
  results_df,
  normalized_counts,
  sample_info,
  p = 0.05,
  lfc = 0.58,
  exp_name,
  ctrl_name,
  top_n = NULL
)
}
\arguments{
\item{results_df}{Data frame containing (annotated) DESeq2 results. If a
\code{SYMBOL} column is present and \code{top_n} is set, gene symbols are used
as row labels.}

\item{normalized_counts}{Genes x Samples numeric matrix of normalized
counts (typically the output of \code{\link[=run_tmm]{run_tmm()}}).}

\item{sample_info}{Data frame with at least two columns: \code{sample}
(canonical IDs matching the \code{make.names()} form of
\code{colnames(normalized_counts)}) and \code{condition} (group label).}

\item{p}{P-value threshold for significance. Default \code{0.05}.}

\item{lfc}{Log2 fold change threshold. Default \code{0.58} (≈1.5x).}

\item{exp_name}{Experimental group name (or vector of names).}

\item{ctrl_name}{Control group name (or vector of names).}

\item{top_n}{If non-NULL, take the top N genes by ascending \code{padj} after
significance filtering, and label rows by gene \code{SYMBOL} when
available. If \code{NULL} (default), include every gene that passes
thresholds with no row labels.}
}
\value{
\code{NULL} if filtering yields fewer than 2 samples or 2 genes;
otherwise a list with \code{zscores} (the z-scored matrix, Genes x
comparison_samples) and \code{row_labels} (gene-symbol labels for the
top-N variant, or \code{NA} for the all-significant variant).
}
\description{
Internal helper extracted from \code{\link[=generate_heatmap]{generate_heatmap()}} for
testability. Performs the significance filter, restricts to the
comparison's exp+ctrl samples (per the Phase 1.2 sample-subset fix),
and z-score normalizes.
}
\keyword{internal}
