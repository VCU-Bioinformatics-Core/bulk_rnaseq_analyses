% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plots_pca.R
\name{.compute_pca}
\alias{.compute_pca}
\title{Compute prcomp on a TMM matrix (internal)}
\usage{
.compute_pca(tmm, rank = NULL)
}
\arguments{
\item{tmm}{Genes x Samples numeric matrix (typically \code{\link[=run_tmm]{run_tmm()}}).}

\item{rank}{Optional integer passed to \code{prcomp(rank. = ...)}. The
3D plot uses \code{rank = 3}; the 2D path leaves it \code{NULL}.}
}
\value{
List with elements \code{prcomp} (the \code{prcomp} object) and
\code{var_pct} (numeric, percent variance per component, rounded to 1 dp).
}
\description{
Drop all-zero rows, transpose so samples are rows, run
\code{stats::prcomp} and capture the percent-variance vector.
}
\keyword{internal}
