% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/io.R
\name{.canonicalize_samplesheet_cols}
\alias{.canonicalize_samplesheet_cols}
\title{Normalize samplesheet column names to the canonical set}
\usage{
.canonicalize_samplesheet_cols(df)
}
\arguments{
\item{df}{Samplesheet data frame.}
}
\value{
\code{df} with canonical column names.
}
\description{
Rename known header variants (see \link{.samplesheet_aliases}) to
the canonical \code{SampleID} / \code{GroupID} / \code{DisplayName} / \code{Exclude} names the
pipeline uses. Without this a header like \verb{Sample_ID,Group_ID} parses
fine but yields \code{samplesheet$GroupID == NULL}, so every contrast silently
finds no groups and the run dies much later with the misleading
"No contrasts to analyse" error.

Contrast columns (anything containing \verb{_vs_}) are never renamed, and a
variant is skipped if its canonical name is already present, so no
duplicate columns can be created.
}
\keyword{internal}
